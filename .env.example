# Copie para .env e ajuste conforme necessário.

# Chave de sessão do Flask. Se não definida, uma chave aleatória é gerada
# a cada início (as sessões não sobrevivem a um restart).
# Gere uma com: python -c "import secrets; print(secrets.token_hex(32))"
APP_SECRET_KEY=
